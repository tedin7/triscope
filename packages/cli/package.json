{
  "name": "@triscope/cli",
  "version": "0.0.0",
  "description": "Triscope CLI: dev, state, list, smoke. Headed-Chromium WebGPU smoke harness for AI iteration.",
  "type": "module",
  "bin": {
    "triscope": "./bin/triscope.mjs"
  },
  "files": ["bin", "src"],
  "license": "MIT",
  "publishConfig": { "access": "public" },
  "dependencies": {
    "pngjs": "^7.0.0"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  },
  "scripts": {
    "build": "echo \"cli: no build\" && exit 0",
    "typecheck": "echo \"cli: pure ESM JS, no typecheck\" && exit 0",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": ["triscope", "cli", "three.js", "webgpu", "claude-code"]
}
